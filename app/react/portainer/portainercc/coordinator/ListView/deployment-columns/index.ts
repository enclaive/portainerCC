import { id } from "./id";
import { coordinatorId } from "./coordinatorId"
import { environment } from "./environment";
import { status } from "./status";

export const deploymentColumns = [
  id,
  coordinatorId,
  status,
  environment
];

